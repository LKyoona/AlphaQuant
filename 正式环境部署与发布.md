# 正式环境部署与发布

正式域名和服务器统一配置在：`deploy\config\production.psd1`。

项目固定名称为 `lhqb`。日常发布读取配置中的 `Domain`，不要再用域名命名脚本、服务或服务器目录。

真正更换域名时，除了修改 `Domain`，还需要同步更新 DNS、HTTPS 证书和服务器上的 Nginx 站点配置；项目文件名和服务名始终保持 `lhqb` 不变。

服务器项目目录：`/data/lhqb`

本方案使用 Nginx、PHP 8.1-FPM、MySQL、Redis 和 Python，不使用 Docker，不使用 Apache。

## 一、日常发布

### 1. 发布前检查

确认代码已保存。修改 PHP 时先检查语法：

```powershell
D:\php74\php.exe -l "PHP文件完整路径"
```

修改 H5 时，先在 `lhqb\h5` 执行：

```powershell
npm run generate
```

并确认构建结果已更新到 `lhqb\public\app`。

### 2. 一键发布

```powershell
cd "G:\量化生产环境\AlphaQuant"
powershell -ExecutionPolicy Bypass -File ".\deploy\scripts\发布正式环境.ps1"
```

脚本会打包项目、排除服务器配置和运行数据、上传、检查 PHP、创建新版本、切换 `current`、重载服务并验收。验收失败时会自动尝试恢复旧版本。

成功时显示：

```text
5/5 发布成功：https://<当前正式域名>
```

## 二、服务器目录

```text
/data/lhqb/current               当前运行版本
/data/lhqb/releases              历史发布版本
/data/lhqb/shared/data/conf      数据库、Redis、邮件等正式配置
/data/lhqb/shared/data/runtime   缓存和运行数据
/data/lhqb/shared/public/upload  用户上传文件
/data/lhqb/logs                  统一日志
/root/lhqb                       指向 /data/lhqb 的软链接
```

查看当前版本：

```bash
readlink -f /data/lhqb/current
```

发布脚本不会覆盖 `shared` 中的正式配置、上传文件和运行数据。

## 三、发布后验收

浏览器检查：

- `https://<当前正式域名>`
- `https://<当前正式域名>/app/`
- `https://<当前正式域名>/admin`
- 一个实际使用的 API 接口

服务器检查：

```bash
/usr/local/sbin/lhqb-health-check
tail -n 100 /data/lhqb/logs/nginx/lhqb-error.log
tail -n 100 /data/lhqb/logs/php/error.log
tail -n 100 /data/lhqb/logs/application/health-check.log
```

成功标准：网站能打开、后台能登录、API 返回正确 JSON，并且日志没有新的 `PHP Fatal`、`Uncaught`、`SQLSTATE` 或 `500`。

## 四、页面缓存与服务重载

后台模板发布后仍显示旧页面：

```bash
rm -rf /data/lhqb/shared/data/runtime/temp/*
systemctl reload php8.1-fpm
```

然后浏览器按 `Ctrl + F5`。

手动重载：

```bash
nginx -t
systemctl reload nginx
systemctl reload php8.1-fpm
```

不要删除整个 `runtime`，只清理 `temp` 或确认可清理的缓存目录。

## 五、一键回滚

本地 PowerShell 执行：

```powershell
cd "G:\量化生产环境\AlphaQuant"
powershell -ExecutionPolicy Bypass -File ".\deploy\scripts\回滚正式环境.ps1"
```

成功时显示：

```text
回滚成功：https://<当前正式域名>
```

回滚后必须重新打开网站，并检查健康检查和错误日志。

## 六、首次部署说明

正式服务器当前已经完成基础环境搭建。若以后更换全新服务器，需要重新配置 Nginx、PHP-FPM、MySQL、Redis、Python、HTTPS、systemd 定时任务、备份和 `/data/lhqb` 目录结构。

现有仓库中包含相关脚本，但没有一条已经确认可在任意新正式服务器无人工检查直接执行的“全自动初始化”命令。首次迁移正式服务器时，需要按照实际服务器、域名、数据库和安全策略逐项确认，不能直接照搬测试机参数。
