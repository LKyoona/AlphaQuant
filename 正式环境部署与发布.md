# 正式环境部署与发布（小白版）

这份文档只解决一件事：代码修改完成后，如何安全发布到当前正式服务器。

如果你不确定该选哪个命令，始终使用“完整一键发布”。它会同时处理 H5、PHP 后台、PHP API 和 Python，最省心。

## 1. 先记住这几个目录

本地项目根目录：

```text
G:\量化生产环境\AlphaQuant
```

本地业务源码目录：

```text
G:\量化生产环境\AlphaQuant\lhqb
```

服务器统一目录：

```text
/data/lhqb/current      当前正在运行的版本
/data/lhqb/previous     上一个版本
/data/lhqb/releases     历史发布版本
/data/lhqb/shared       数据库配置、运行配置、上传文件和缓存
/data/lhqb/logs         所有主要日志
/data/lhqb/backups      自动和手动备份
/root/lhqb              指向 /data/lhqb 的快捷软链接
```

发布配置文件：

```text
G:\量化生产环境\AlphaQuant\deploy\config\production.psd1
```

这个文件保存服务器地址、域名、项目名和 SSH 私钥路径，不保存服务器密码。

## 2. 第一次在这台电脑发布前检查

只需要检查一次，全部通过后，日常发布可以直接看第 4 节。

### 2.1 打开 PowerShell

按 `Win` 键，搜索 `PowerShell` 并打开。

### 2.2 检查项目目录

```powershell
Test-Path "G:\量化生产环境\AlphaQuant\lhqb"
```

显示 `True` 才是正确的。

### 2.3 检查 Node.js 和 npm

```powershell
node -v
npm -v
```

都能显示版本号即可。Node.js 用于构建 H5。

### 2.4 检查 SSH 私钥

```powershell
Test-Path "$env:USERPROFILE\.ssh\test_machine_ed25519"
```

显示 `True` 才能免密发布。不要把这个私钥上传到 Git，也不要发给其他人。

### 2.5 测试 SSH 连接

命令会直接读取 `production.psd1`，以后服务器地址变化时只改配置文件，不需要改文档：

```powershell
$Config = Import-PowerShellDataFile "G:\量化生产环境\AlphaQuant\deploy\config\production.psd1"
$SshKey = Join-Path $env:USERPROFILE $Config.SshKey
ssh -i $SshKey $Config.Server "echo SSH_OK"
```

看到 `SSH_OK` 表示连接正常。

如果服务器重装后提示主机指纹变化，先向服务器提供商确认新指纹，再删除旧记录，不能在未确认时直接忽略安全警告。

## 3. 发布前判断自己改了什么

| 修改内容 | 常见目录 | 推荐发布方式 |
|---|---|---|
| H5 页面 | `lhqb\h5` | 完整一键发布 |
| PHP API | `lhqb\api` | 完整一键发布；熟悉后可跳过 H5 |
| PHP 后台逻辑 | `lhqb\app` | 完整一键发布；熟悉后可跳过 H5 |
| 后台管理页面 | `lhqb\public\themes\admin_h` | 完整一键发布；熟悉后可跳过 H5 |
| Python 行情、爬虫或交易 | `lhqb\python` | 完整一键发布；熟悉后可跳过 H5 |
| 多个目录一起修改 | 任意组合 | 完整一键发布 |
| 数据库表结构或数据 | MySQL/SQL | 一键发布不会自动执行，需要单独备份和迁移 |

注意：发布脚本打包的是你本地磁盘上的当前文件。即使代码没有提交 Git，也会被发布，所以发布前一定先看一眼：

```powershell
cd "G:\量化生产环境\AlphaQuant"
git status --short
```

## 4. 完整一键发布（新手只用这个）

打开 PowerShell，复制执行：

```powershell
cd "G:\量化生产环境\AlphaQuant"
powershell -ExecutionPolicy Bypass -File ".\deploy\scripts\发布正式环境.ps1"
```

脚本会自动完成：

1. 检查 H5 的 npm 依赖；没有依赖时自动执行 `npm ci`。
2. 执行 `npm run generate` 构建 H5。
3. 把 `lhqb\h5\.output\public` 同步到 `lhqb\public\app`。
4. 打包整个 `lhqb` 项目。
5. 上传发布包和服务器部署脚本。
6. 在服务器检查所有 PHP 文件语法。
7. Python 依赖发生变化时更新虚拟环境。
8. 发布前自动备份数据库和共享文件。
9. 创建新的 `/data/lhqb/releases/时间戳` 版本。
10. 原子切换 `/data/lhqb/current`。
11. 重载 Nginx、PHP，重启行情和交易服务。
12. 请求正式 API 做健康检查；失败时自动尝试恢复旧版本。

正常结束时会看到类似：

```text
DEPLOYMENT=PASS
CURRENT_RELEASE=/data/lhqb/releases/时间戳
7/7 发布成功：https://当前域名
```

只要中途出现红色报错或没有看到 `DEPLOYMENT=PASS`，就不能认为发布成功。

## 5. 快速发布（确定没改 H5 时使用）

如果只修改了 PHP、后台模板或 Python，并且确定不需要重新构建 H5，可以执行：

```powershell
cd "G:\量化生产环境\AlphaQuant"
powershell -ExecutionPolicy Bypass -File ".\deploy\scripts\发布正式环境.ps1" -SkipH5
```

`-SkipH5` 只是不重新构建 H5，PHP、后台模板、Python 和现有 `public\app` 仍会一起打包发布。

不确定时不要使用 `-SkipH5`，直接执行第 4 节的完整一键发布。

## 6. 发布后怎么验收

### 6.1 先看当前版本

```powershell
$Config = Import-PowerShellDataFile "G:\量化生产环境\AlphaQuant\deploy\config\production.psd1"
$SshKey = Join-Path $env:USERPROFILE $Config.SshKey
ssh -i $SshKey $Config.Server "readlink -f /data/lhqb/current"
```

应该显示刚才发布输出里的同一个时间戳版本。

### 6.2 浏览器检查

从 `deploy\config\production.psd1` 读取当前域名，然后至少检查：

1. 网站首页能打开。
2. `/app/` H5 能打开并能登录。
3. `/admin` 后台能登录并打开本站用户页面。
4. 本次修改的页面或接口符合预期。
5. 浏览器按 `F12`，Network 中没有新增的 `404` 或 `500`。

### 6.3 一键健康检查

先登录服务器：

```powershell
$Config = Import-PowerShellDataFile "G:\量化生产环境\AlphaQuant\deploy\config\production.psd1"
$SshKey = Join-Path $env:USERPROFILE $Config.SshKey
ssh -i $SshKey $Config.Server
```

进入服务器后执行：

```bash
/usr/local/sbin/lhqb-health-check
tail -n 30 /data/lhqb/logs/application/health-check.log
```

最后看到 `HEALTH_CHECK=PASS` 才算通过。

### 6.4 检查主要服务

```bash
systemctl is-active nginx php8.1-fpm mysql redis-server
systemctl is-active lhqb-market.service lhqb-trading.service lhqb-smtp-mss.service
systemctl is-active lhqb-news-crawler.timer lhqb-backup.timer lhqb-health-check.timer
```

每一项都应该返回 `active`。

## 7. 日志在哪里看

| 问题 | 命令 |
|---|---|
| PHP 报错、页面错误、接口 500 | `tail -f /data/lhqb/logs/php/error.log` |
| Nginx 404、反代或访问错误 | `tail -f /data/lhqb/logs/nginx/lhqb-error.log` |
| 自动健康检查 | `tail -f /data/lhqb/logs/application/health-check.log` |
| Python 行情 | `tail -f /data/lhqb/logs/market/market.log` |
| Python 正式交易 | `tail -f /data/lhqb/logs/trading/trading.log` |
| 新闻爬虫 | `tail -f /data/lhqb/logs/crawler/news-crawler.log` |
| 邮件网络辅助服务 | `journalctl -u lhqb-smtp-mss.service -n 100 --no-pager` |

停止实时查看日志时按 `Ctrl + C`。

交易日志约定：

- H5/API 能看到的机器人状态和交易记录保存为英文。
- `/data/lhqb/logs/trading/trading.log` 保持中文，方便自己排查。
- 不需要配置语言参数。

快速搜索严重错误：

```bash
grep -R -E "PHP Fatal|Uncaught|SQLSTATE|Internal Server Error" /data/lhqb/logs --exclude='*.gz'
```

## 8. 后台页面还是旧的怎么办

先在浏览器按 `Ctrl + F5`。

如果仍然是旧页面，再登录服务器执行：

```bash
rm -rf /data/lhqb/shared/data/runtime/temp/*
systemctl reload php8.1-fpm
```

只清理 `runtime/temp`，不要删除整个 `/data/lhqb/shared/data/runtime`。

## 9. 一键回滚

如果发布后出现无法接受的问题，在本地 PowerShell 执行：

```powershell
cd "G:\量化生产环境\AlphaQuant"
powershell -ExecutionPolicy Bypass -File ".\deploy\scripts\回滚正式环境.ps1"
```

回滚脚本会：

1. 把 `current` 切换到 `previous`。
2. 重载 Nginx 和 PHP。
3. 重启行情与交易服务。
4. 验证正式 API。
5. 验收失败时恢复回滚前的版本。

回滚成功后仍要重复第 6 节的验收步骤。

重要：代码回滚不会自动撤销数据库表结构和业务数据变更。涉及数据库迁移时，必须提前准备对应的数据库回退方案。

## 10. 如何撤回刚才的回滚

回滚脚本会交换 `current` 和 `previous`，所以需要恢复到回滚前的版本时，再执行一次同样的回滚命令即可：

```powershell
cd "G:\量化生产环境\AlphaQuant"
powershell -ExecutionPolicy Bypass -File ".\deploy\scripts\回滚正式环境.ps1"
```

执行后必须重新验收，不能只看脚本提示。

## 11. 常见失败怎么处理

### SSH 连接失败

先执行：

```powershell
$Config = Import-PowerShellDataFile "G:\量化生产环境\AlphaQuant\deploy\config\production.psd1"
$SshKey = Join-Path $env:USERPROFILE $Config.SshKey
ssh -i $SshKey $Config.Server "echo SSH_OK"
```

如果失败，检查网络、服务器地址、私钥路径和服务器 SSH 服务。

### `npm` 找不到

安装 Node.js，关闭旧 PowerShell，重新打开后执行：

```powershell
node -v
npm -v
```

### H5 构建失败

进入 H5 目录单独运行，先看完整错误：

```powershell
cd "G:\量化生产环境\AlphaQuant\lhqb\h5"
npm ci
npm run generate
```

### PHP 页面错误或接口 500

服务器执行：

```bash
tail -n 100 /data/lhqb/logs/php/error.log
tail -n 100 /data/lhqb/logs/nginx/lhqb-error.log
```

不要在没有日志依据时反复重启所有服务。

### 发布时间较长

完整发布需要构建 H5、打包和上传整个项目、检查全部 PHP、备份数据库、检查 Python 依赖并重启服务，所以通常比复制单个文件慢，但可以避免漏文件和无法回滚。

## 12. 哪些内容不会被发布覆盖

下面这些正式数据保存在 `shared`，正常发布不会被代码包覆盖：

```text
/data/lhqb/shared/data/conf
/data/lhqb/shared/data/runtime
/data/lhqb/shared/public/upload
/data/lhqb/shared/python
```

有一个明确例外：发布脚本会把本地 `lhqb\data\conf\email.php` 更新到服务器邮件配置位置。发布前不要误删或使用错误的本地邮件配置。

## 13. 换全新服务器时怎么办

这份一键发布脚本用于“已经初始化好的正式服务器”，不能直接给一台空白 Ubuntu 安装 MySQL、Redis、Nginx、PHP、证书和全部 systemd 服务。

更换全新服务器时，需要重新确认以下信息：

- Ubuntu 版本和软件源
- 新服务器 IP、SSH 用户和密钥
- 域名 DNS 与 HTTPS 证书
- MySQL 数据迁移和账号权限
- Redis 配置
- Nginx 与 PHP-FPM 配置
- `/data/lhqb/shared` 私有配置
- 上传文件和数据库备份恢复
- Python 虚拟环境与 systemd 服务
- 防火墙和交易所 API IP 白名单

仓库目前没有一条已经验证能适配任意空白服务器的全自动初始化命令。因此换新服务器属于“首次环境搭建”，需要向运维确认，不能把日常发布脚本当安装脚本使用。

## 14. 新手每次发布只记这 5 步

1. 保存代码。
2. 执行 `git status --short`，确认没有误改文件。
3. 执行第 4 节的完整一键发布命令。
4. 看到 `DEPLOYMENT=PASS` 和 `7/7 发布成功`。
5. 打开网站验收，并执行健康检查；有问题立即查看日志或回滚。
