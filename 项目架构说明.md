# 项目架构说明

## 1. 项目是什么

这是一个前后端不完全分离的系统：

- H5 使用 Nuxt 4、Vue 3 和 Vant。
- API、后台控制器和业务逻辑使用 PHP、ThinkPHP/ThinkCMF。
- 后台管理页面使用 PHP 模板 `admin_h`，不是单独的前端工程。
- MySQL 保存业务数据。
- Redis 保存缓存、验证码和限流等短期数据。
- Python 当前用于新闻爬虫，并为后续逐步迁移 PHP 功能预留空间。
- Nginx 统一提供静态文件、H5 和 PHP 请求入口。

## 2. 目录关系

| 目录 | 作用 |
|---|---|
| `lhqb\api` | H5 调用的 PHP API 模块 |
| `lhqb\app` | ThinkCMF 后台、用户、门户等 PHP 模块 |
| `lhqb\data\conf` | 本地数据库、Redis、邮件等配置 |
| `lhqb\h5` | Nuxt/Vue H5 源码 |
| `lhqb\public\app` | H5 构建后的静态发布文件 |
| `lhqb\public\api` | API Web 入口相关文件，不是 `lhqb\api` 源码的重复副本 |
| `lhqb\public\themes\admin_h` | 当前唯一使用的后台管理模板 |
| `lhqb\public\static` | 公共 CSS、JS、图片等静态资源 |
| `lhqb\python` | Python 程序和新闻爬虫 |
| `lhqb\system` | ThinkPHP/ThinkCMF 框架代码，非必要不要修改 |
| `deploy` | 发布、回滚、初始化、SQL 和 systemd 配置 |

## 3. 请求如何流转

### H5 页面

```text
浏览器 -> Nginx -> public/app 静态文件 -> /api 接口
```

### PHP API

```text
浏览器/H5 -> Nginx -> PHP-FPM -> public/index.php 或 API 入口 -> api/app 业务代码 -> MySQL/Redis
```

### 后台管理系统

```text
浏览器 /admin -> Nginx -> PHP-FPM -> app 中的控制器 -> public/themes/admin_h 模板 -> MySQL
```

因此，后台功能通常同时涉及：

1. `app` 中的控制器、模型或权限逻辑。
2. `public/themes/admin_h` 中的页面模板。
3. MySQL 中的菜单、角色和权限数据。

只修改模板不能解决后端权限或数据问题；只修改 PHP 也不能自动改变页面布局。

## 4. H5 源码与发布文件

`lhqb\h5` 是源码，浏览器不会直接读取它。执行 `npm run generate` 后，生成静态文件，再同步到 `lhqb\public\app`，服务器才会显示新版本。

不要把 `lhqb\api` 和 `lhqb\public\api` 当成同一个目录：前者主要是业务源码，后者位于 Web 根目录，承担公开入口或兼容资源的职责。

## 5. 服务器运行结构

测试和正式环境统一采用版本目录：

```text
/data/lhqb/releases/时间戳版本
/data/lhqb/current -> 当前版本
/data/lhqb/shared -> 不随发布覆盖的数据
/data/lhqb/logs -> 日志
/root/lhqb -> /data/lhqb
```

发布时创建新版本并切换 `current`，配置、上传和缓存通过 `shared` 保留。出现问题时可以把 `current` 切回上一版本。

## 6. 配置和数据原则

- 本地配置在 `lhqb\data\conf`。
- 服务器真实配置在 `/data/lhqb/shared/data/conf`。
- 数据库名统一为 `lhqb`。
- 密码、SMTP 应用密码和第三方 API 密钥不得写入文档。
- `deploy\sql\lhqb-seed.sql` 只用于全新数据库补充基础数据，日常发布不要执行。
- 后续数据库结构变更必须先备份正式数据库，使用用途明确且可重复执行的 SQL，并在执行后立即验收。

## 7. Python 后续迁移原则

目前不需要一次性把 PHP 全部重写为 Python。更稳的方式是：

1. 保持 Nginx 作为统一入口。
2. 新功能可以先以独立 Python 服务实现。
3. 通过清晰的 HTTP API 与现有 PHP 系统交互。
4. 每迁移一个模块，就补接口验收、日志和回滚方案。
5. 数据库字段和业务规则保持兼容，避免一次性大改造成全站风险。

这种方式允许 PHP 与 Python 并行运行，后续逐步替换，不影响当前系统继续开发和发布。
