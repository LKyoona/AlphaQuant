# 本地开发说明

## 1. 需要准备什么

本地开发通常需要下面这些东西：

- Windows PowerShell
- PHP 7.4，当前使用 `D:\php74\php.exe`
- MySQL，数据库名固定为 `lhqb`
- Node.js 和 npm，修改 H5 时需要

PHP 至少要启用这些扩展：

- `pdo_mysql`
- `mysqli`
- `openssl`
- `mbstring`

检查 PHP 扩展：

```powershell
D:\php74\php.exe -m
```

## 2. 启动本地 PHP

在 PowerShell 执行：

```powershell
cd "G:\量化生产环境\AlphaQuant\lhqb\public"
D:\php74\php.exe -S 127.0.0.1:8888 router.php
```

服务启动后，浏览器可以访问：

- 首页：`http://127.0.0.1:8888`
- 后台：`http://127.0.0.1:8888/admin`
- 接口：`http://127.0.0.1:8888/api/...`

停止服务时，在 PowerShell 按 `Ctrl + C`。

## 3. 数据库配置

配置文件：

```text
G:\量化生产环境\AlphaQuant\lhqb\data\conf\database.php
```

本地一般这样配置：

```text
主机：127.0.0.1
端口：3306
数据库：lhqb
```

不要把正式环境数据库密码写进文档，也不要提交到公开仓库。

## 4. 后台怎么改

后台不是单独的前端工程，而是 PHP 模板和控制器一起运行。

| 内容 | 位置 |
|---|---|
| 后台控制器 | `lhqb\app\admin`、`lhqb\app\user` |
| 后台页面 | `lhqb\public\themes\admin_h` |
| 公共静态资源 | `lhqb\public\static` |
| 统一入口 | `lhqb\public\index.php` |

修改 HTML 模板后，刷新后台页面即可。

如果还是旧页面：

1. 先按 `Ctrl + F5`
2. 再清理 `lhqb\data\runtime\temp`
3. 不要删除整个 `runtime`

## 5. H5 怎么改和构建

H5 源码目录：

```text
G:\量化生产环境\AlphaQuant\lhqb\h5
```

首次安装依赖：

```powershell
cd "G:\量化生产环境\AlphaQuant\lhqb\h5"
npm install
```

本地开发：

```powershell
npm run dev:all
```

生成发布文件：

```powershell
npm run generate
```

Nuxt 的生成结果通常在 `lhqb\h5\.output\public`。

发布前要确认最新文件已经同步到：

```text
G:\量化生产环境\AlphaQuant\lhqb\public\app
```

如果构建输出目录变了，以这次命令实际输出为准，不要凭猜测覆盖目录。

## 6. PHP 语法检查

修改 PHP 后先检查语法：

```powershell
D:\php74\php.exe -l "PHP文件完整路径"
```

看到 `No syntax errors detected` 才能发布。

## 7. 本地正常、服务器异常时先查什么

优先检查下面几项：

1. 本地和服务器的 PHP 版本是否不同。
2. 数据库结构和数据是否一致。
3. 服务器 `data/conf` 配置是否完整。
4. H5 是否已经重新构建。
5. Nginx、PHP 和应用日志里有没有新的错误。
