# 本地开发说明

## 1. 本地需要什么

- Windows PowerShell
- PHP 7.4：当前使用 `D:\php74\php.exe`
- MySQL：数据库名 `lhqb`
- Node.js 和 npm：修改 H5 时需要

PHP 至少启用 `pdo_mysql`、`mysqli`、`openssl`、`mbstring`。执行下面命令检查：

```powershell
D:\php74\php.exe -m
```

## 2. 启动 PHP 项目

在 PowerShell 执行：

```powershell
cd "G:\量化生产环境\AlphaQuant\lhqb\public"
D:\php74\php.exe -S 127.0.0.1:8888 router.php
```

保持窗口不要关闭，然后访问：

- 首页：`http://127.0.0.1:8888`
- 后台：`http://127.0.0.1:8888/admin`
- API：`http://127.0.0.1:8888/api/...`

停止服务时，在 PowerShell 按 `Ctrl + C`。

## 3. 数据库配置

配置文件：

```text
G:\量化生产环境\AlphaQuant\lhqb\data\conf\database.php
```

本地通常使用：

```text
主机：127.0.0.1
端口：3306
数据库：lhqb
```

不要把正式环境数据库密码写进文档或提交到公开仓库。

## 4. 修改后台管理系统

后台不是独立前端工程，而是 PHP 模板与控制器一起运行。

| 内容 | 位置 |
|---|---|
| 后台控制器 | `lhqb\app\admin`、`lhqb\app\user` |
| 后台页面 | `lhqb\public\themes\admin_h` |
| 公共静态资源 | `lhqb\public\static` |
| 统一入口 | `lhqb\public\index.php` |

修改 HTML 模板后刷新后台即可。若仍显示旧页面，按 `Ctrl + F5`；必要时清理 `lhqb\data\runtime\temp`，不要删除整个 `runtime`。

## 5. 修改和构建 H5

H5 源码目录：

```text
G:\量化生产环境\AlphaQuant\lhqb\h5
```

首次安装依赖：

```powershell
cd "G:\量化生产环境\AlphaQuant\lhqb\h5"
npm install
```

本地开发：

```powershell
npm run dev:all
```

生成发布文件：

```powershell
npm run generate
```

Nuxt 生成结果通常在 `lhqb\h5\.output\public`。发布前必须确认最新文件已经同步到：

```text
G:\量化生产环境\AlphaQuant\lhqb\public\app
```

如果构建输出目录发生变化，以本次命令实际输出为准，不要凭猜测覆盖目录。

## 6. PHP 语法检查

修改 PHP 后先检查：

```powershell
D:\php74\php.exe -l "PHP文件完整路径"
```

看到 `No syntax errors detected` 才能发布。

## 7. 本地正常、服务器异常时

优先检查：

1. 本地和服务器 PHP 版本是否不同。
2. 数据库结构和数据是否一致。
3. 服务器 `data/conf` 配置是否完整。
4. H5 是否已经重新构建。
5. Nginx、PHP 和应用日志是否有新错误。
